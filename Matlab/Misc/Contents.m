% MISC
%
% Files
%   arbitrary_alpha  - function s_alpha = arbitrary_alpha( s, alpha )
%   clip_fill_scale  - clips or zero-fills a row or column vector to a prescribed length
%   clip_or_fill     - clips or zero-fills a row or column vector to a prescribed length
%   corr_align       - ************************************************
%   corr_align1      - ************************************************
%   corr_align2      - ************************************************
%   dist_wh          - DIST    Computes distance and bearing between points on the earth
%   distance         - Calculates the distance between two geographical points.
%   ExampDopplerComp - 
%   findfirst        - findfirst( x)
%   funct            - defines function for root finder
%   latlon_to_m      - usage [ x, y ] = lat_lon_to_meters( lat, lon, lat_O, lon_O )
%   LoadBathymetry   - Loads the bathymetry file
%   m_to_latlon      - usage [ lat, lon ] = m_to_latlon( x, y, lat_O, lon_O )
%   modified_chirp_z - function X = modified_chirp_z( x, M, delta_phi, phi_0 )
%   next_radix2      - 
%   noise_maker      - usage: noise = noise_maker( power,len )
%   pcmread          - reads a 'PCM' file (intel, little endian)
%   pcmwrite         - write a 'PCM' file (intel, little endian)
%   rise_align       - usage: r_align = rise_align( r, Threshold, Offset )
%   sec              - Secant method
%   thorpe           - thorpe attenuation
%   whiten           - usage: y = whiten( x )
